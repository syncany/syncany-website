insert into app set
  `type` = :type,
  `appVersion` = :appVersion,
  `os` = :os,
  `arch` = :arch,
  `date` = :date,
  `release` = :release,
  `checksum` = :checksum,
  `basename` = :basename,
  `fullpath` = :fullpath