insert into app set
  `dist` = :dist,
  `type` = :type,
  `appVersion` = :appVersion,
  `os` = :os,
  `arch` = :arch,
  `date` = :date,
  `release` = :release,
  `checksum` = :checksum,
  `basename` = :basename,
  `fullpath` = :fullpath